# Local (non-Docker) development only.
# The browser never uses this — it's the server-side target the Next.js dev
# server proxies /api/* to. Defaults to http://localhost:4000 if unset.
BACKEND_URL=http://localhost:4000
