## What changed

<!-- What this PR does, and why — the why matters more than the what,
     the diff already shows the what. -->

## Related issue

<!-- Closes #123, or "None" -->

## How I tested this

<!-- Steps you actually ran. For frontend UI changes, attach a screenshot
     or short clip. For backend changes, say which endpoint, with what
     request, and what you confirmed in the response/DB — there's no
     backend test suite yet, so this is the actual verification. -->

## Checklist

- [ ] `npm run lint` passes in `frontend/` (if frontend changed)
- [ ] If this touches money/pricing, auth, or returns/exchange logic: I
      re-read the relevant rule in `CONTRIBUTING.md` and this PR follows it
- [ ] If this touches the barcode scanner or ESC/POS printing: I re-read
      the relevant README section and didn't change the documented
      behavior without updating it
- [ ] Screenshots attached for any `frontend/` UI change
